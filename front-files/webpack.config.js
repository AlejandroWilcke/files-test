const path = require('path');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  entry: './src/index.js',
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: 'bundle.js'
  },
  mode: 'development',
  module: {
    rules: [
      {
        test: /\.(js|jsx)$/,
        exclude: /node_modules/,
        use: {
          loader: 'babel-loader'
        }
      },
      {
        test: /\.css$/,
        use: ['style-loader', 'css-loader']
      }
    ]
  },
  resolve: {
    extensions: ['.js', '.jsx']
  },
  plugins: [
    new HtmlWebpackPlugin({
      template: './public/index.html'
    })
  ],
  cache: false,
  devServer: {
    static: './dist',
    hot: true, // Enable Hot Module Replacement
    watchFiles: ['src/**/*'], // Watch the source files
    port: 3000,
    liveReload: true, // Ensure live reload is enabled
    client: {
      overlay: true, // Show errors/warnings in browser overlay
    },
  }
};
